logging:
  path: ${user.home}/logs/dispatcher
  file:
    max-size: 50MB
server:
  port: 8081
#  servlet:
#    context-path: /dispatcher
spring:
  profiles:
    active: dev
  jpa:
    open-in-view: false
#  application:
#    name: USERSERVICE


logging:
  path: ${user.home}/logs/kulu-gas-deploy
  file:
    max-size: 50MB
server:
  port: 8081
spring:
  profiles:
    active: dev
  jpa:
    open-in-view: false
  application:
    name: USERSERVICE
